{
  "name": "@rxjs-insights/devtools-extension",
  "version": "0.2.0",
  "private": true,
  "scripts": {
    "start": "node build.js",
    "run": "web-ext run --target chromium --start-url http://localhost:3000"
  },
  "devDependencies": {
    "web-ext": "^6.8.0",
    "@types/chrome": "^0.0.180"
  }
}
