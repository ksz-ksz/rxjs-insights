import { styled } from '@mui/material';

export const TargetsPanelDiv = styled('div')({
  display: 'flex',
  flexDirection: 'column',
  height: '100%',
  whiteSpace: 'nowrap',
  paddingRight: '1rem',
  cursor: 'default',
});
